# Port du serveur de développement backend
PORT=3000

# Clé API OpenAI pour les analyses IA personnalisées (Optionnel - Fallback intégré)
OPENAI_API_KEY=

# Clé et URL Supabase pour stocker les données en base cloud (Optionnel - Fallback JSON local intégré)
SUPABASE_URL=
SUPABASE_KEY=
